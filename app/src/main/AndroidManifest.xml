<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codembeded.jutttravelsco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Tours"></activity>
        <activity android:name=".activity.MyHistory" />
        <activity android:name=".activity.BookMyTicket" />
        <activity android:name=".activity.BookMyRide" />
        <activity android:name=".activity.BookTour" />
        <activity android:name=".activity.Complain" />
        <activity android:name=".activity.MyBookings" />
        <activity android:name=".activity.SignUp" />
        <activity android:name=".activity.Splash" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>